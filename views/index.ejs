<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <link rel='stylesheet' href='/stylesheets/markdown.css' />
    <link rel='stylesheet' href='/stylesheets/jquery-linedtextarea.css' />
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.8.1/css/all.css" integrity="sha384-50oBUHEmvpQ+1lW4y57PTFmhCaXp0ML5d60M1M7uH2+nqUivzIebhndOJK28anvf" crossorigin="anonymous">
    <link rel="icon" type="image/png" href="images/logo.png" />
  </head>
  <body>
    <div class="container">
      <h1 class="text-center"><img src="/images/logo.png" height="37" alt="logo"> Markdown converter</h1>
      <div class="row">
        <div class="col-md-6 col-sm-12">
          <form id="form" method="post">
            <textarea id="text" class="textarea" name="text" rows="10"></textarea>
            <div class="btn-group" style="width:100%;" role="group">
              <button type="submit" class="btn btn-info"><i class="fas fa-recycle"></i> Convertir</button>
              <button type="button" class="btn btn-info" data-toggle="modal" data-target="#mdDoc"><i class="fas fa-book"></i> Documentation Markdown</button>
            </div>
          </form>
        </div>
        <div class="col-md-6 col-sm-12">
          <div id="wrapper"></div>
          <form method="POST" id="htmlForm">
            <textarea name="htmlMD" id="htmlMD" style="display:none"></textarea>
            <div class="btn-group" style="width:100%;" role="group">
              <button id="pdfExport" type="submit" class="btn btn-info"><i class="fas fa-file-pdf"></i> Exporter en .pdf</button>
              <button id="mdExport" type="submit" class="btn btn-info"><i class="fab fa-markdown"></i> Exporter en .md</button>
            </div>
          </form>
        </div>

      </div>
    </div>

  <!-- Footer -->
  <footer class="page-footer font-small mt-5" id="footer-wrapper">

      <!-- Footer Elements -->
      <div class="container">

        <!-- Social buttons -->
        <ul class="list-unstyled list-inline text-center">
          <li class="list-inline-item">
            <a class="mx-1 text-info" href="https://fr.linkedin.com/in/emilien-sechilariu" target="_blank">
              <i class="fab fa-linkedin-in"></i>
            </a>
          </li>
          <li class="list-inline-item">
            <a class="mx-1 text-info" href="https://github.com/EmilienSE" target="_blank">
              <i class="fab fa-github"></i>
            </a>
          </li>
        </ul>
        <!-- Social buttons -->

      </div>
      <!-- Footer Elements -->

      <!-- Copyright -->
      <div class="footer-copyright text-center py-3">
        <a href="http://esechilariu.fr/" target="_blank" class="btn btn-info">Emilien SECHILARIU</a>
      </div>
      <!-- Copyright -->

    </footer>
    <!-- Footer -->


    <div class="modal fade" id="mdDoc" tabindex="-1" role="dialog">
      <div class="modal-dialog modal-xl" role="document">
        <div class="modal-content">
          <div class="modal-header">
            <h5 class="modal-title">Documentation Markdown</h5>
            <button type="button" class="close" data-dismiss="modal" aria-label="Close">
              <span aria-hidden="true">&times;</span>
            </button>
          </div>
          <div class="modal-body">
            <h1>Guide de la syntaxe</h1>

            <p>Voici la documentation de la syntaxe Markdown.</p>

            <h2>En-têtes</h2>

            <pre class="highlight">
              <code>
                <span># Voici un tag &lt;h1&gt;</span>
                <span>## Voici un tag &lt;h2&gt;</span>
                <span>###### Voici un tag &lt;h6&gt;</span>
              </code>
            </pre>

            <h3>Emphasis</h3>

            <div class="language-markdown highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="ge">*This text will be italic*</span>
            <span class="ge">_This will also be italic_</span>

            <span class="gs">**This text will be bold**</span>
            <span class="gs">__This will also be bold__</span>

            <span class="ge">_You **can** combine them_</span>
            </code></pre></div></div>

            <h3>Lists</h3>

            <h4>Unordered</h4>

            <div class="language-markdown highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">*</span> Item 1
            <span class="p">*</span> Item 2
            <span class="p">  *</span> Item 2a
            <span class="p">  *</span> Item 2b
            </code></pre></div></div>

            <h4>Ordered</h4>

            <div class="language-markdown highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">1.</span> Item 1
            <span class="p">1.</span> Item 2
            <span class="p">1.</span> Item 3
            <span class="p">   1.</span> Item 3a
            <span class="p">   1.</span> Item 3b
            </code></pre></div></div>

            <h3>Images</h3>

            <div class="language-markdown highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">![</span><span class="nv">GitHub Logo</span><span class="p">](</span><span class="sx">/images/logo.png</span><span class="p">)</span>
            Format: !<span class="p">[</span><span class="nv">Alt Text</span><span class="p">](</span><span class="sx">url</span><span class="p">)</span>
            </code></pre></div></div>

            <h3>Links</h3>

            <div class="language-markdown highlighter-rouge"><div class="highlight"><pre class="highlight"><code>http://github.com - automatic!
            <span class="p">[</span><span class="nv">GitHub</span><span class="p">](</span><span class="sx">http://github.com</span><span class="p">)</span>
            </code></pre></div></div>

            <h3>Blockquotes</h3>

            <div class="language-markdown highlighter-rouge"><div class="highlight"><pre class="highlight"><code>As Kanye West said:
            <span class="gt">
            &gt; We're living the future so</span>
            <span class="gt">&gt; the present is our past.</span>
            </code></pre></div></div>

            <h3>Inline code</h3>

            <div class="highlighter-rouge"><div class="highlight"><pre class="highlight"><code>I think you should use an
            `&lt;addr&gt;` element here instead.
            </code></pre></div></div>

          </div>
          <div class="modal-footer">
            <button type="button" class="btn btn-secondary" data-dismiss="modal">Fermer</button>
          </div>
        </div>
      </div>
    </div>


    <script src="https://code.jquery.com/jquery-3.3.1.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js" integrity="sha384-UO2eT0CpHqdSJQ6hJty5KVphtPhzWj9WO1clHTMGa3JDZwrnQq4sF86dIHNDz0W1" crossorigin="anonymous"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js" integrity="sha384-JjSmVgyd0p3pXB1rRibZUAYoIIy6OrQ6VrjIEaFf/nJGzIxFDsf4x0xIM+B07jRM" crossorigin="anonymous"></script>
    <script src="/javascripts/jquery-linedtextarea.js"></script>
    <script>

      $(function() {
        $('#text').linedtextarea();
      });

      $('#form').submit(function(event){
        event.preventDefault();
        var text = $('#form').serialize();
        $.ajax({
          url: '/mdown',
          type: 'POST',
          data: text,
          processData: false
        })
        .done(function(response){
          $('#wrapper').empty();
          $('#htmlMD').val('');
          $('#wrapper').append(response);
          $('#htmlMD').val(response);
        });
      });

      $('#pdfExport').click(function(event){
        event.preventDefault();
        $('#pdfExport').prepend('<head><link rel=\'stylesheet\' type=\'text/css\' href=\'/stylesheets/markdown.css\' /></head>');
        var textHtml = $('#htmlForm').serialize();
        $.ajax({
          url: '/pdfExport',
          method: 'POST',
          data: textHtml,
          //dataType: 'text/html',
          processData: false
        })
        .done(function(response, status, xhr){
          console.log(response);

          var filename = "";
          var disposition = xhr.getResponseHeader('Content-Disposition');
          if (disposition && disposition.indexOf('attachment') !== -1) {
              var filenameRegex = /filename[^;=\n]*=((['"]).*?\2|[^;\n]*)/;
              var matches = filenameRegex.exec(disposition);
              if (matches != null && matches[1]) { 
                filename = matches[1].replace(/['"]/g, '');
              }
          }

          var element = document.createElement('a');
          element.setAttribute('href', filename);
          element.setAttribute('download', filename);

          element.style.display = 'none';
          document.body.appendChild(element);

          element.click();

          console.log(element);

          document.body.removeChild(element);

        });
      });

      $('#mdExport').click(function(event){
        event.preventDefault();
        var textMD = $('#text').val();
        var element = document.createElement('a');
        element.setAttribute('href', 'data:text/plain;charset=utf-8,' + encodeURIComponent(textMD));
        element.setAttribute('download', 'conversion.md');

        element.style.display = 'none';
        document.body.appendChild(element);

        element.click();

        document.body.removeChild(element);
      })

    </script>
  </body>
</html>
