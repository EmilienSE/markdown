<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <link rel='stylesheet' href='/stylesheets/markdown.css' />
    <link rel='stylesheet' href='/stylesheets/jquery-linedtextarea.css' />
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.8.1/css/all.css" integrity="sha384-50oBUHEmvpQ+1lW4y57PTFmhCaXp0ML5d60M1M7uH2+nqUivzIebhndOJK28anvf" crossorigin="anonymous">
  </head>
  <body>
    <div class="container">
      <h1 class="text-center"><img src="/images/logo.png" height="37" alt="logo"> Markdown converter</h1>
      <div class="row">
        <div class="col-md-6 col-sm-12">
          <form id="form" method="post">
            <textarea id="text" class="textarea" name="text" rows="10"></textarea>
            <button type="submit" class="btn btn-info btn-lg btn-block"><i class="fas fa-recycle"></i> Convertir</button>
          </form>
        </div>
        <div class="col-md-6 col-sm-12">
          <div id="wrapper"></div>
          <div class="btn-group" style="width:100%;" role="group">
            <button id="pdfExport" class="btn btn-info"><i class="fas fa-file-pdf"></i> Exporter en .pdf</button>
            <button id="mdExport" class="btn btn-info"><i class="fab fa-markdown"></i> Exporter en .md</button>
          </div>
        </div>


      </div>
    </div>


    <script src="https://code.jquery.com/jquery-3.3.1.min.js"></script>
    <script src="/javascripts/jquery-linedtextarea.js"></script>
    <script>
      $(function() {
        $('#text').linedtextarea();
      });

      $('#form').submit(function(event){
        event.preventDefault();
        var text = $('#form').serialize();
        $.ajax({
          url: '/mdown',
          type: 'POST',
          data: text,
          processData: false
        })
        .done(function(response){
          console.log(response);
          $('#wrapper').empty();
          $('#wrapper').append(response);
        });
      });
    </script>
  </body>
</html>
